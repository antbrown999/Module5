// Exercise 7 pt 2
class logger {
    constructor(Referrer, id) {
        this.referrer = this.referrer
        this.id = id
    }

    log = (value) => {
        console.log(`[${this.referrer}:${this.id}`)
    }

}